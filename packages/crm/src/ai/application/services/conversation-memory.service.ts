import { ConversationMemory } from '../../domain/value-objects/conversation-memory.vo';
export class ConversationMemoryService {
  updateMemory(_memory: ConversationMemory, _newData: unknown): void {
    // update logic
  }
}
