import { CRMContext } from '../../domain/value-objects/crm-context.vo';

export class CRMResolver {
  static resolveContext(data: unknown): CRMContext {
    // Placeholder resolution
    return data as CRMContext;
  }
}
